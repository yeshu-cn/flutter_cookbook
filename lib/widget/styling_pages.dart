
import 'package:flutter/material.dart';

class StylingPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Styling"),
      ),
    );
  }

}