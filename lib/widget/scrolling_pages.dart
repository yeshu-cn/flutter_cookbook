
import 'package:flutter/material.dart';

class ScrollingPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Scrolling"),
      ),
    );
  }

}