
import 'package:flutter/material.dart';

class InteractionModelsPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Interaction Models"),
      ),
    );
  }

}